/** Shown above server-paginated admin grids. */
export const ADMIN_GRID_CLIENT_FILTER_HINT =
  'Toolbar search and column filters apply to the current page only.';

/** Inline fragment for table summaries (lowercase lead-in). */
export const ADMIN_GRID_CLIENT_FILTER_HINT_INLINE =
  'toolbar search and column filters apply to the current page';
